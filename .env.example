# Copie para .env e preencha com valores reais. Nunca commite o .env de verdade.

# Postgres (Neon). Em dev local sem Neon ainda, use `npx prisma dev` e cole a URL que ele imprimir.
DATABASE_URL=""

# Segredo de assinatura de sessão do Auth.js — gere um valor próprio:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""
